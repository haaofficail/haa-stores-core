/**
 * Shared types for landing page sections.
 *
 * Extracted from LandingPage.tsx (P2-#1 refactor).
 * TFn is the i18n translation function signature used across all sections.
 */
export type TFn = (key: string, fallback?: string) => string;
