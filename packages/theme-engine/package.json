{
  "name": "@haa/theme-engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "HAA Theme Engine — Pure Commerce theme infrastructure (contracts, validation, registry, sandbox)",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "4.1.8",
    "zod": "^3.23.8"
  }
}
