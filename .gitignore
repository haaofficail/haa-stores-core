node_modules
dist
.env
*.tsbuildinfo
.next
coverage
.turbo
.vite
storage

# macOS Finder metadata noise
.DS_Store

# Log files (avoid tracking accidental local logs)
*.log
**/*.log

# Playwright test artifacts (regenerated on every run)
tests/e2e/test-results/
tests/e2e/report/
tests/e2e/screenshots/
.hostinger-mcp.env

# Production secrets — never commit
deploy/production/.env
deploy/staging/.env

# Local secrets
.env
.env.*
!.env.example
.hostinger-mcp.env

# Claude / agent temporary worktrees
.claude/worktrees/
.worktrees/

# Generated build artifacts
packages/theme-web/.next/
packages/theme-web/storybook-static/

# Local backups
CLAUDE.md.backup.*
